-- Флаг: пользователь создан/синхронизирован как руководитель отдела из Битрикса.
-- Нужен, чтобы при синхронизации деактивировать ТОЛЬКО тех, кто перестал быть
-- руководителем, и не трогать обычных пользователей.
ALTER TABLE t_p67567221_one_file_page_projec.users
    ADD COLUMN IF NOT EXISTS is_bitrix_head BOOLEAN DEFAULT FALSE;